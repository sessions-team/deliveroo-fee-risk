# secrets / per-machine config — VM-only, never in git
.env
.env.*
!.env.example
allowed-emails.json
*.bak.*

# dependencies
node_modules/

# generated (fully rebuildable from data/raw + data/reference)
output/

# runtime artefacts
logs/
uploads/
